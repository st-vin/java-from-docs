/*Basic Interface Definition (Easy)

**Concept**: Defining an Interface

Create an interface called `Drawable` with a single method `draw()` that returns void.
 */

public interface Drawable {

    public void draw();
}